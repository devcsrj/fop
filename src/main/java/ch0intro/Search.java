package ch0intro;

public class Search {

    /**
     * Iterates over the array and looks for the highest number
     *
     * @param numbers the array to search for
     * @return the highest number
     */
    public int findHighest( int[] numbers ) {
        return 0;
    }

    /**
     * Iterates over the array and looks for the lowest number
     *
     * @param numbers the array to search for
     * @return the lowest number
     */
    public int findLowest( int[] numbers ) {
        return 0;
    }
}
