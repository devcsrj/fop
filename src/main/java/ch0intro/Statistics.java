package ch0intro;

import java.util.Arrays;

public class Statistics {

    /**
     * To find the mean:
     *  - Add all the numbers
     *  - Divide the sum by the number of elements
     *
     * @param numbers an array of numbers
     * @return the mean of numbers
     */
    public double computeMean(double[] numbers) {
        return 0; // FIXME
    }

    /**
     * To find the median number:
     * - Put all the numbers in numerical order.
     * - If there is an odd number of results, the median is the middle number.
     * - If there is an even number of results, the median will be the mean of the two central numbers.
     *
     * @param numbers a sorted array of number (no need to sort in numerical order)
     * @return the median of numbers
     */
    public double computeMedian(double[] numbers) {
        return 0; // FIXME
    }

    /**
     * To find the mode:
     * - Find the number that is repeated is most often
     * - If multiple numbers are repeated the same number of times, return both
     *
     * @param numbers an array of numbers
     * @return the mode of numbers
     */
    public double[] computeMode(double[] numbers) {
        return new double[0]; // FIXME
    }
}
